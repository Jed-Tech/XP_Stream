# Minecraft 26.1.1 Update Plan (XP_Stream)

Plan for updating the workspace to the `26.1.1` line, moving **NeoForge** to `26.1.1.1-beta`, validating **Fabric** on the same family, and releasing **XP_Stream 1.1.1**.

**Status:** Executed (2026-04-03)  
**Scope:** repo-wide loader/tooling verification for both mods; only `xp_stream` gets the `1.1.1` mod version bump in this pass

---

## Goals

- Keep the repo aligned with the Minecraft `26.1.1` family.
- Update **NeoForge** from `26.1.0.5-beta` to `26.1.1.1-beta`.
- Re-verify **Fabric** against the same `26.1.1` update line.
- Keep both `xp_stream` and `saturation_regen` buildable after the root pin changes.
- Bump `mods/xp_stream/gradle.properties` from `mod_version=1.1.0` to `mod_version=1.1.1`.
- Record release notes and compatibility after testing.

---

## Current -> target

| Item | Current | Target |
|------|---------|--------|
| `minecraft_version` | `26.1` | `26.1.1` if resolvable for Fabric tooling; otherwise use the exact 26.1.1-compatible identifier that resolves in Loom |
| `neoforge_version` | `26.1.0.5-beta` | `26.1.1.1-beta` |
| `mods/xp_stream/gradle.properties` `mod_version` | `1.1.0` | `1.1.1` |
| `mods/saturation_regen/gradle.properties` `mod_version` | `0.1.0` | No version bump planned; must still build after root pin changes |
| Fabric verification | Built for `26.1` | Rebuild and smoke-test on `26.1.1` line |
| NeoForge verification | Built for `26.1.0.5-beta` | Rebuild and smoke-test on `26.1.1.1-beta` |

---

## Files expected to change

- `gradle.properties`
- `mods/xp_stream/gradle.properties`
- `CHANGELOG.md`
- Optional docs touched during execution if any compatibility notes need to be updated

---

## Planned changes

### 1. Root version pins

Update repo-wide properties in `gradle.properties`:

- Set `neoforge_version=26.1.1.1-beta`
- Move `minecraft_version` to the matching `26.1.1` family value used for local dev/builds
- Keep Java 25
- Treat these as workspace-wide pins: both `xp_stream` and `saturation_regen` must continue to compile on Fabric and NeoForge after the change
- Leave Fabric properties unchanged unless the `26.1.1` update requires newer Fabric loader/Loom/API values during build verification

### 2. Fabric dependency resolution check

Before locking the final root pins, explicitly verify Fabric-side resolution for the `26.1.1` line:

- Confirm the `minecraft_version` value chosen in `gradle.properties` resolves in Loom
- Confirm whether `fabric_loader_version`, `fabric_loom_version`, and `fabric_api_version` can stay pinned as-is
- If Fabric API or related tooling needs a newer `26.1.1`-compatible build, update the plan and pins before smoke testing

### 3. XP_Stream release version

Update `mods/xp_stream/gradle.properties`:

- Change `mod_version=1.1.0` -> `mod_version=1.1.1`

Expected effect:

- Fabric jar version becomes `1.1.1`
- NeoForge jar version becomes `1.1.1`
- Expanded loader metadata follows the same mod version

### 4. Metadata sanity check

Confirm existing broad compatibility policy still holds:

- `mods/xp_stream/fabric/src/main/resources/fabric.mod.json`
- `mods/xp_stream/neoforge/src/main/resources/META-INF/neoforge.mods.toml`
- `mods/saturation_regen/fabric/src/main/resources/fabric.mod.json`
- `mods/saturation_regen/neoforge/src/main/resources/META-INF/neoforge.mods.toml`
- `Docs/broad_minecraft_family_policy.md`

Checks:

- Fabric dependency ranges still make sense for the `26.1.1` line
- NeoForge minimum version expands correctly from `${neoforge_version}`
- No loader metadata needs tightening beyond the compile pin

### 5. Changelog

Add a new top entry to `CHANGELOG.md` for `1.1.1` that captures:

- Minecraft `26.1.1` update support
- NeoForge update to `26.1.1.1-beta`
- Fabric compatibility/test status
- Any user-visible behavior changes if they appear during verification

---

## Verification plan

Use `just` commands from the repo root.

### Build checks

1. `just build-all`
2. Confirm both mods still build under the new root pins:
   - `xp_stream`
   - `saturation_regen`
3. If needed, target XP_Stream loader runs individually:
   - `just xp-stream-fabric-client`
   - `just xp-stream-neoforge-client`

### What to verify

- Fabric build completes successfully
- NeoForge build completes successfully with `26.1.1.1-beta`
- `saturation_regen` remains buildable on both loaders after the root pin update
- XP orb burst pickup behavior still works
- No new mixin/config/runtime startup errors
- Output artifacts reflect `1.1.1`

Artifact expectation examples:

- `mods/xp_stream/fabric/build/libs/...1.1.1.jar`
- `mods/xp_stream/neoforge/build/libs/...1.1.1.jar`

---

## Risks / watch items

- Fabric may still resolve against a slightly different version string than plain `26.1.1`; use the resolvable value required by Loom if necessary
- Fabric API may need a `26.1.1`-compatible update even if the current `26.1` pin looks close
- NeoForge patch updates can require a matching `minecraft_version` change in root `gradle.properties`
- ModDevGradle may also need review if the NeoForge bump expects a newer companion version
- If Fabric or NeoForge updated mappings/tooling surface API drift, code changes may be needed even if this starts as a version-only bump

---

## Done checklist

- [x] `gradle.properties` updated for the `26.1.1` line
- [x] Fabric-side resolution checked for `minecraft_version`, Loom, loader, and Fabric API
- [x] `neoforge_version=26.1.1.1-beta`
- [x] `mods/xp_stream/gradle.properties` set to `mod_version=1.1.1`
- [x] `CHANGELOG.md` has a new `1.1.1` entry
- [x] `just build-all` succeeds
- [x] `saturation_regen` still builds under the updated root pins
- [x] `just xp-stream-fabric-client` smoke-tested
- [x] `just xp-stream-neoforge-client` smoke-tested
- [x] Built artifacts show version `1.1.1`

---

## Execution notes

- `just build-all` succeeded on 2026-04-03 with both `xp_stream` and `saturation_regen` building on Fabric and NeoForge.
- Fabric client launched successfully; user confirmed the orb absorption test went well.
- NeoForge client launched successfully; user confirmed the orb absorption test went well.
- Built artifacts confirmed:
  - `mods/xp_stream/fabric/build/libs/xp_stream-fabric-26.1.1-1.1.1.jar`
  - `mods/xp_stream/neoforge/build/libs/xp_stream-neoforge-26.1.1-1.1.1.jar`
