{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "Server-side natural regen from saturation without a full hunger bar, gated by a configurable minimum food level. Preserves vanilla cadence and exhaustion.",
  "authors": ["Jed-Tech"],
  "license": "CC-BY-NC-ND-4.0",
  "icon": "assets/saturation_regen/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.jedtech.saturation_regen.fabric.SaturationRegenFabricMod"
    ]
  },
  "mixins": [
    "saturation_regen.fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.16.0",
    "minecraft": ">=1.21.1"
  }
}
