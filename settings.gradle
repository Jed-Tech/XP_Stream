pluginManagement {
    repositories {
        maven {
            url = "https://maven.neoforged.net/releases"
        }
        maven {
            name = "Fabric"
            url = "https://maven.fabricmc.net/"
        }
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        // Phase 2: Using Loom 1.15.0-alpha.25 for Minecraft 26.1 snapshot-2 (unobfuscated)
        // Using net.fabricmc.fabric-loom for unobfuscated versions (26.1+)
        // Note: Version resolved from gradle.properties via fabric/build.gradle
    }
}

rootProject.name = "XP_Stream"
include("common", "fabric", "neoforge")
