# Minecraft 26.1 GA — final update plan (XP_Stream)

Plan for moving the repo from pre-release pins to **Minecraft 26.1 GA** tooling, releasing **mod version 1.0.0**, and documenting the release.

**References:** [Fabric for Minecraft 26.1](https://fabricmc.net/2026/03/14/261.html), [NeoForge for Minecraft 26.1](https://neoforged.net/news/26.1release/).

**Status — executed (2026-03-24):** Pins, `mod_version` **1.0.0**, `CHANGELOG` **`[1.0.0]`**, Gradle **9.4.0**, Fabric Loom **1.15.5** (`settings.gradle` + `gradle.properties`), Fabric/NeoForge **client** smoke tests OK on **Minecraft 26.1**. Still optional: §6 publishing project IDs, **`git tag v1.0.0`** / GitHub release.

---

## Goals

- Build and smoke-test **Fabric** and **NeoForge** against current **26.1 GA** targets.
- Ship **XP_Stream 1.0.0** (semantic “stable for 26.1 GA” release).
- Update **user-facing** release notes in `CHANGELOG.md`.

---

## 1. Mod version → 1.0.0

- **File:** `mods/xp_stream/gradle.properties`
- **Change:** set `mod_version=1.0.0` (replaces current `0.9.0`).
- **Effect:** Gradle `project.version`, JAR names (`*-fabric-*`, `*-neoforge-*`), and expanded `fabric.mod.json` / `neoforge.mods.toml` all follow this value.

**Optional:** `saturation_regen` has its own `mod_version`; only bump if you intend to release that mod on the same cadence.

---

## 2. Update `CHANGELOG.md` (required for 1.0.0)

Add a **new top section** `[1.0.0]` with a **user-centric** summary (same tone as existing entries):

- What players get (e.g. official 26.1 GA support, same burst-pickup behavior).
- **Compatibility (tested):** list Fabric + NeoForge + Minecraft **26.1** after you actually run smoke tests.

**Do not** skip this step if you publish or tag 1.0.0; the changelog is the public release story.

---

## 3. Repo-wide version pins (`gradle.properties` + wrapper)

**Note:** The table below is the **migration reference** (old → target). Canonical **current** values live in root `gradle.properties`, `gradle/wrapper/gradle-wrapper.properties`, and **`settings.gradle`** (Fabric Loom plugin version must match the intended Loom release).

Apply the following (adjust if a newer Fabric API / NeoForge patch ships before you merge).

| Property | Current | Suggestion |
|----------|---------|------------|
| `minecraft_version` | `26.1-pre-3` | `26.1` — use only if Loom resolves `com.mojang:minecraft:26.1`; if Gradle fails, use the exact GA id from the launcher manifest or Fabric’s example mod. |
| `neoforge_version` | `26.1.0.0-alpha.15+pre-3` | `26.1.0.1-beta` |
| `moddevgradle_version` | `2.0.140` | `2.0.141` |
| `fabric_loader_version` | `0.18.4` | `0.18.4` — recheck [Fabric Loader releases](https://github.com/FabricMC/fabric-loader/releases) for anything newer before release. |
| `fabric_loom_version` | `1.15.0-alpha.25` | `1.15.5` |
| `fabric_api_version` | `0.141.2+26.1` | `0.144.0+26.1` — recheck [Fabric API releases](https://github.com/FabricMC/fabric-api/releases) for a build explicitly targeting GA if available. |
| Gradle wrapper (`gradle-wrapper.properties`) | `9.2.1` | `9.4.0` — Fabric’s 26.1 post recommends 9.4.0 with Loom 1.15; 9.2.1 may still work. |

**Root `build.gradle`:** Java **25** toolchain is already required for 26.1; keep aligned with Fabric/NeoForge docs.

**Comments in `gradle.properties`:** update the pre-3 / alpha commentary so it describes GA pins, not pre-release-only.

---

## 4. Loader metadata sanity check

Standing policy for broad Minecraft ranges: `Docs/broad_minecraft_family_policy.md`.

After changing `neoforge_version`, `neoforge.mods.toml` expands `versionRange = "[${neoforge_version},)"` for NeoForge — minimum loader matches the compile pin (expected).

- **Minecraft** range `[26.1-,)` should remain valid for the broad 26.1 family.
- **Fabric** `fabric.mod.json`: confirm `minecraft` and `fabricloader` constraints still match your intent (`>=26.1-alpha.2` and `>=0.18.4` are usually still fine for GA).

---

## 5. Build and verify

1. **Clean + compile:**  
   `.\gradlew :mods:xp_stream:fabric:build :mods:xp_stream:neoforge:build`  
   (or full repo build if you want `saturation_regen` configured too.)

2. **Resolve failures:**  
   - If **Fabric** cannot find Minecraft `26.1`, fix `minecraft_version` to the resolvable id.  
   - If **NeoForge** / ModDevGradle errors, confirm `neoforge_version` + `moddevgradle_version` pair matches NeoForge’s current recommendation.

3. **Smoke test:**  
   - Fabric client/server: burst pickup, no mixin errors.  
   - NeoForge client/server: same. Reusing an existing world is fine; NeoForge may warn about version drift—usually ignorable for behavior checks.

4. **Artifacts:**  
   - Fabric: `mods/xp_stream/fabric/build/libs/xp_stream-fabric-<mc>-1.0.0.jar`  
   - NeoForge: `mods/xp_stream/neoforge/build/libs/xp_stream-neoforge-<mc>-1.0.0.jar`

---

## 6. Publishing (if applicable)

- `mods/xp_stream/gradle.properties`: set `modrinth_project_id` / `curseforge_project_id` when ready.  
- Publish tasks use root `minecraft_version` in version display strings — ensure it reflects what you ship (e.g. `26.1`).

---

## 7. Done checklist

- [x] `mod_version=1.0.0` in `mods/xp_stream/gradle.properties`
- [x] Root `gradle.properties` + wrapper (+ `settings.gradle` Loom **1.15.5**) updated per §3
- [x] `CHANGELOG.md` — `[1.0.0]` entry + tested compatibility (Fabric + NeoForge client)
- [x] Fabric + NeoForge builds green; client smoke tests on **Minecraft 26.1** GA
- [ ] Optional: tag `v1.0.0` / GitHub release; §6 publishing when project IDs and tokens are set
